import { Box, styled } from '@mui/material';

export const HeaderContainer = styled(Box)(() => ({
    display: 'flex',
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: '20px',
}));
